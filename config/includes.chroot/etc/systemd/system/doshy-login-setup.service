[Unit]
Description=doshy OS login setup (show the login screen, set a password if needed)
After=live-config.service
Before=display-manager.service

[Service]
Type=oneshot
ExecStart=/usr/local/lib/doshy/login-setup
RemainAfterExit=yes

[Install]
WantedBy=display-manager.service
